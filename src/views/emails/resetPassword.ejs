<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DealSpot OTP Verification</title>
    <style>
      body {
        font-family: 'Segoe UI', sans-serif;
        background-color: #fdf2f8;
        margin: 0;
        padding: 20px;
        color: #333;
      }

      .container {
        max-width: 550px;
        margin: 30px auto;
        background-color: #fff;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
        padding: 40px 30px;
        border: 1px solid #f8d7da;
      }

      .logo {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        object-fit: cover;
        margin-bottom: 10px;
      }

      .brand-name {
        font-size: 22px;
        font-weight: bold;
        color: #d63384;
        margin-bottom: 15px;
      }

      h2 {
        color: #d63384;
        font-size: 24px;
        margin-bottom: 10px;
      }

      p {
        font-size: 15px;
        color: #555;
        margin-bottom: 20px;
        line-height: 1.6;
      }

      .otp-box {
        display: inline-block;
        background-color: #ffe3ec;
        color: #d63384;
        font-size: 28px;
        font-weight: bold;
        padding: 15px 25px;
        border-radius: 12px;
        letter-spacing: 10px;
        border: 2px dashed #f06595;
        margin: 20px 0;
      }

      .footer {
        font-size: 13px;
        color: #888;
        margin-top: 30px;
        border-top: 1px solid #eee;
        padding-top: 15px;
      }
    </style>
  </head>
  <body>
    <div class="container" align="center">
      <!-- Logo -->
      <img src="../../../public/asset/deal_spot_log.png" alt="DealSpot Logo" class="logo" />

      <!-- Brand Name -->
      <div class="brand-name">DealSpot</div>

      <!-- Heading -->
      <h2>Password Reset OTP</h2>

      <!-- Greeting -->
      <p>Hello <strong><%= name %></strong>,</p>

      <!-- Instructions -->
      <p>
        Use the OTP below to reset your password for your <strong>DealSpot</strong> account.
        This OTP is valid for <strong> 5 minutes</strong>.
      </p>

      <!-- OTP Display -->
      <div class="otp-box"><%= otp %></div>

      <!-- Footer Note -->
      <p style="margin-top: 25px;">
        If you did not request this password reset, you can safely ignore this email.
      </p>

      <!-- Footer -->
      <div class="footer">
        © <%= new Date().getFullYear() %> DealSpot. All rights reserved.
      </div>
    </div>
  </body>
</html>
